# cert_openssl

```python
python --path output --url example.lilly.com --method key+csr
```


```bash
--path -p path to output certificate file, default="cert_files"
--url -u SAN, default="example.lilly.com"
--method -m key+csr or key+csr+cert, default="key+csr"

